<?xml version="1.0" encoding="utf-8"?>
<!--
  The app never makes an outbound request. Cleartext is permitted only so that the embedded
  server can serve the phone controller over plain HTTP on the local network, which is exactly
  what the specification describes: the secure QR token is the access mechanism, and the app
  does not claim the LAN connection is encrypted.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
